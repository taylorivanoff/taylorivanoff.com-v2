pipeline:
  build:
    image: docker
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    commands:
      - docker compose build
      - docker compose up -f docker-compose.yml -f docker-compose.prod.yml --no-deps -d
      - docker compose exec app ls -la
